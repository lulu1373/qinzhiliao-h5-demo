# V3.3 抽屉边缘修复

根因：CSS 实际宽度 min(88%,388px) 与 JS 关闭距离 min(86%,380px) 不一致，并叠加常驻阴影。

修复：单一宽度来源、自身百分比平移、关闭态不可见/不可点击/无阴影、遮罩与主页面复位、消除待执行拖动帧及关闭回调竞态。

业务流程和页面设计不变。

验收与原始证据见 tests/DRAWER_EDGE_DIAGNOSIS_V3.3.md。
