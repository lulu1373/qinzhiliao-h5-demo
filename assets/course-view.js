(function(root, factory) {
  const api = factory();
  if (typeof module !== 'undefined' && module.exports) module.exports = api;
  if (root) root.QZLCourseView = api;
})(typeof window !== 'undefined' ? window : globalThis, function() {
  'use strict';
  const money = fen => `¥${(Number(fen || 0) / 100).toFixed(2)}`;
  const mins = seconds => `${Math.max(1, Math.round(Number(seconds || 0) / 60))} 分钟`;
  const icon = name => ({
    book:'<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 5.5A2.5 2.5 0 0 1 6.5 3H11v16H6.5A2.5 2.5 0 0 0 4 21.5v-16Zm16 0A2.5 2.5 0 0 0 17.5 3H13v16h4.5a2.5 2.5 0 0 1 2.5 2.5v-16Z"/></svg>',
    play:'<svg viewBox="0 0 24 24" aria-hidden="true"><path d="m9 7 8 5-8 5V7Z"/></svg>',
    coin:'<svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="8"/><path d="M9.5 9.5h5M9 12h6M12 7.5v9"/></svg>',
    gift:'<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 10h16v10H4V10Zm-1-4h18v4H3V6Zm9 0v14M12 6c-1.5-3-5-3-5 0h5Zm0 0c1.5-3 5-3 5 0h-5Z"/></svg>',
    lock:'<svg viewBox="0 0 24 24" aria-hidden="true"><rect x="5" y="10" width="14" height="10" rx="2"/><path d="M8 10V7a4 4 0 0 1 8 0v3"/></svg>',
    clock:'<svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/></svg>',
    check:'<svg viewBox="0 0 24 24" aria-hidden="true"><path d="m5 12 4 4L19 6"/></svg>'
  }[name] || '');
  const getProduct = (ctx,id) => ctx.model.CATALOG.find(item => item.id === id);
  const parts = route => String(route || '').split('?')[0].split('/').filter(Boolean);
  const query = route => Object.fromEntries(new URLSearchParams(String(route || '').split('?')[1] || ''));
  const esc = (ctx,value) => ctx.esc ? ctx.esc(value) : String(value || '');
  const scrollPage = html => String(html || '').replace('<main class="course-page','<main class="course-page qzl-page-scroll');
  const title = (text, back='courses') => `<header class="course-titlebar"><button data-course-action="back" data-fallback="${back}" aria-label="返回">‹</button><b>${text}</b><span></span></header>`;
  const detailButton = product => `<button class="course-card" data-course-action="detail" data-product-id="${product.id}"><span class="course-cover type-${product.type}">${icon(product.type === 'points' ? 'coin' : product.type === 'newcomer' ? 'gift' : 'book')}</span><span class="course-card-copy"><small>${product.topic}</small><b>${product.title}</b><em>${product.subtitle}</em></span><i>›</i></button>`;

  function homeEntry(ctx) {
    return '';
  }
  function drawerEntry(ctx) {
    const count = ctx.data.entitlements.filter(item => item.status === 'active').length;
    return `<button class="course-drawer-tool classroom" data-course-action="route" data-route="courses"><span class="course-drawer-icon">${icon('book')}</span><b>简快课堂</b><small>亲子与家庭课程</small></button><button class="course-drawer-tool learning" data-course-action="route" data-route="my-learning"><span class="course-drawer-icon">${icon('play')}</span><b>我的学习</b><small>${count ? `已获取 ${count} 门` : '继续已获取课程'}</small></button>`;
  }
  function classroom(ctx) {
    const newcomer = getProduct(ctx,'B001');
    const member = ctx.data.membership;
    const memberText = member.active ? `本期还有 ${Math.max(0,member.quota-member.used)} 个 ¥9.9 名额` : '开通会员后，可购买更多 ¥9.9 课程';
    const owned = ctx.data.entitlements.filter(item => item.status === 'active').map(item => getProduct(ctx,item.productId)).filter(Boolean);
    const cards = ctx.model.CATALOG.filter(item => item.type !== 'newcomer');
    return `<main class="course-page">${title('简快课堂','home')}<section class="course-hero"><span>${icon('book')}</span><div><small>简快课堂 · 演示课程</small><h1>从听懂彼此，走到一起做到</h1><p>课程内容为演示示例，获取流程不会产生真实扣款。</p></div></section>${owned.length ? `<section class="course-section"><div class="course-section-head"><h2>继续学习</h2><button data-course-action="route" data-route="my-learning">全部 ›</button></div>${detailButton(owned[0])}</section>` : ''}<section class="course-section"><div class="course-section-head"><h2>新注册用户</h2><span class="course-price-chip">新人 ¥9.9</span></div>${detailButton(newcomer)}</section><section class="course-member-strip"><span>${icon('gift')}</span><div><b>会员专享课</b><small>${memberText}</small></div><button data-course-action="route" data-route="membership">查看权益</button></section><section class="course-section"><div class="course-section-head"><h2>成长积分兑换</h2><button data-course-action="route" data-route="points">${ctx.data.points.balance} 积分 ›</button></div>${cards.filter(item => item.type === 'points').map(detailButton).join('')}</section><section class="course-section"><div class="course-section-head"><h2>为此刻的家庭问题找方法</h2><button data-course-action="route" data-route="courses/list">查看全部 ›</button></div>${cards.filter(item => item.type === 'member').map(detailButton).join('')}</section></main>`;
  }
  function list(ctx,route) {
    const q = query(route), keyword = String(q.q || '').trim();
    const products = ctx.model.CATALOG.filter(item => !keyword || `${item.title}${item.topic}${item.subtitle}`.includes(keyword));
    return `<main class="course-page">${title('全部课程')}<section class="course-search"><input id="courseSearch" value="${esc(ctx,keyword)}" placeholder="搜索课程主题或关键词"><button data-course-action="search">搜索</button></section><p class="course-count">找到 ${products.length} 门演示课程</p><section class="course-list">${products.map(detailButton).join('') || '<div class="course-empty"><b>暂时没有找到相关课程</b><p>换一个关键词试试。</p></div>'}</section></main>`;
  }
  function detail(ctx,productId) {
    const product = getProduct(ctx,productId); if (!product) return missing(ctx);
    const offer = ctx.model.offerFor(ctx.data,product.id,new Date().toISOString());
    const total = product.lessons.reduce((sum,item) => sum + item.durationSeconds,0);
    const price = offer.method === 'points' ? `${offer.pointsCost} 积分兑换` : offer.amountFen ? money(offer.amountFen) : offer.label;
    const button = offer.reason === 'owned' ? `<button class="course-primary" data-course-action="learn" data-product-id="${product.id}">去学习</button>` : offer.available ? `<button class="course-primary" data-course-action="checkout" data-product-id="${product.id}" data-method="${offer.method}">${offer.method === 'points' ? `兑换课程 · ${offer.pointsCost} 积分` : `获取课程 · ${price}`}</button>` : `<button class="course-primary" disabled>${offer.reason === 'points_insufficient' ? `还差 ${Math.max(0,offer.pointsCost-ctx.data.points.balance)} 积分` : offer.reason === 'member_required' ? '会员专享 ¥9.9' : '当前暂不可获取'}</button>`;
    return `<main class="course-page course-detail">${title('课程详情')}<section class="course-detail-hero type-${product.type}"><span>${icon(product.type === 'points' ? 'coin' : product.type === 'newcomer' ? 'gift' : 'book')}</span><small>${product.topic} · 演示课程</small><h1>${esc(ctx,product.title)}</h1><p>${esc(ctx,product.subtitle)}</p></section><section class="course-price"><div><small>获取方式</small><b>${esc(ctx,offer.label || price)}</b></div><strong>${price}</strong></section><section class="course-detail-block"><h2>你会学到</h2><p>从当下的一次互动出发，找到能在家庭里尝试的一小步。课程为 H5 演示内容，不包含真实讲师音视频。</p></section><section class="course-detail-block"><h2>课程目录 · ${mins(total)}</h2>${product.lessons.map((lesson,index) => `<button class="course-lesson" data-course-action="preview" data-product-id="${product.id}" data-lesson-id="${lesson.id}"><span>${index+1}</span><div><b>${esc(ctx,lesson.title)}</b><small>${mins(lesson.durationSeconds)} · 可试听</small></div><i>›</i></button>`).join('')}</section><footer class="course-sticky"><span>模拟支付，不会扣款</span>${button}</footer></main>`;
  }
  function checkout(ctx,productId,route) {
    const product = getProduct(ctx,productId); if (!product) return missing(ctx);
    const method = query(route).method === 'points' ? 'points' : 'cash';
    const existing = ctx.data.orders.find(item => item.id === query(route).orderId && item.productId === productId);
    if (existing && ['pending','processing'].includes(existing.status)) {
      const value = existing.method === 'points' ? `${existing.pointsCost} 积分` : money(existing.amountFen);
      return `<main class="course-page">${title(existing.method === 'points' ? '确认兑换' : '确认获取',`courses/detail/${product.id}`)}<section class="course-confirm-card"><span>${icon('clock')}</span><h1>${esc(ctx,product.title)}</h1><p>${existing.status === 'processing' ? '正在确认这笔演示订单' : '这笔演示订单等待确认'}</p><dl><div><dt>本次${existing.method === 'points' ? '兑换' : '支付'}</dt><dd>${value}</dd></div><div><dt>订单编号</dt><dd>${esc(ctx,existing.id)}</dd></div></dl></section><p class="course-demo-note">关闭支付面板不会取消订单；只有明确取消才会释放本次演示资格。</p>${existing.status === 'pending' ? `<button class="course-primary full" data-course-action="pay-existing" data-order-id="${existing.id}">继续模拟${existing.method === 'points' ? '兑换' : '支付'}</button><button class="course-secondary full" data-course-action="cancel-order" data-order-id="${existing.id}">取消订单</button>` : `<button class="course-secondary full" data-course-action="route" data-route="courses/orders">查看订单状态</button>`}</main>`;
    }
    const offer = ctx.model.offerFor(ctx.data,product.id,new Date().toISOString());
    if (!offer.available || offer.method !== method) return `<main class="course-page">${title('确认获取',`courses/detail/${product.id}`)}<div class="course-empty"><b>当前获取条件已变化</b><p>请回到课程详情重新确认。</p><button class="course-primary" data-course-action="route" data-route="courses/detail/${product.id}">返回课程详情</button></div></main>`;
    const value = method === 'points' ? `${offer.pointsCost} 积分` : money(offer.amountFen);
    return `<main class="course-page">${title(method === 'points' ? '确认兑换' : '确认获取',`courses/detail/${product.id}`)}<section class="course-confirm-card"><span>${icon('check')}</span><h1>${esc(ctx,product.title)}</h1><p>有效期：获取成功起 365 天</p><dl><div><dt>本次${method === 'points' ? '兑换' : '支付'}</dt><dd>${value}</dd></div><div><dt>${method === 'points' ? '可用积分' : '支付方式'}</dt><dd>${method === 'points' ? `${ctx.data.points.balance} 积分` : '模拟支付'}</dd></div></dl></section><p class="course-demo-note">这是演示环境，点击后不会扣款，也不会调用真实支付服务。</p><button class="course-primary full" data-course-action="pay" data-product-id="${product.id}" data-method="${method}">确认${method === 'points' ? '兑换' : '模拟支付'} · ${value}</button></main>`;
  }
  function result(ctx,orderId) {
    const order = ctx.data.orders.find(item => item.id === orderId);
    const product = order && getProduct(ctx,order.productId);
    if (!order || !product) return missing(ctx);
    const succeeded = order.status === 'succeeded';
    return `<main class="course-page course-result">${title('获取结果','courses')}<section class="course-result-card ${succeeded ? 'success' : ''}"><span>${succeeded ? icon('check') : icon('clock')}</span><h1>${succeeded ? '课程已加入我的学习' : order.status === 'processing' ? '正在确认结果' : '这次获取没有完成'}</h1><p>${esc(ctx,product.title)}</p>${succeeded ? '<small>课程有效期 365 天；会员到期后，已获取课程仍可继续学习。</small>' : '<small>演示订单可从课程详情重新发起。</small>'}</section>${succeeded ? `<button class="course-primary full" data-course-action="learn" data-product-id="${product.id}">开始学习</button>` : `<button class="course-primary full" data-course-action="route" data-route="courses/detail/${product.id}">回到课程详情</button>`}</main>`;
  }
  function learning(ctx) {
    const owned = ctx.data.entitlements.filter(item => item.status === 'active' && new Date(item.validUntil).getTime() > Date.now()).map(item => ({...item,product:getProduct(ctx,item.productId)})).filter(item => item.product);
    return `<main class="course-page">${title('我的学习','home')}<section class="course-learning-head"><span>${icon('play')}</span><div><h1>把理解慢慢放回日常</h1><p>已获取 ${owned.length} 门课程</p></div></section><section class="course-list">${owned.map(item => `<button class="course-card" data-course-action="learn" data-product-id="${item.product.id}"><span class="course-cover type-${item.product.type}">${icon('play')}</span><span class="course-card-copy"><small>有效至 ${String(item.validUntil).slice(0,10)}</small><b>${esc(ctx,item.product.title)}</b><em>从第一节开始学习</em></span><i>›</i></button>`).join('') || '<div class="course-empty"><b>还没有已获取的课程</b><p>去课堂找到适合此刻的一门课。</p><button class="course-primary" data-course-action="route" data-route="courses">去简快课堂</button></div>'}</section></main>`;
  }
  function player(ctx,productId,lessonId) {
    const product = getProduct(ctx,productId), lesson = product?.lessons.find(item => item.id === lessonId) || product?.lessons[0];
    if (!product || !lesson) return missing(ctx);
    if (!ctx.model.canLearn(ctx.data,productId,new Date().toISOString())) return `<main class="course-page">${title('课程学习',`courses/detail/${productId}`)}<div class="course-empty"><span>${icon('lock')}</span><b>先获取课程，再开始完整学习</b><p>你可以在详情页试听第一节内容。</p><button class="course-primary" data-course-action="route" data-route="courses/detail/${productId}">查看课程详情</button></div></main>`;
    return `<main class="course-page course-player">${title('课程学习','my-learning')}<section class="course-video"><span>${icon('play')}</span><b>演示播放器 · 示例内容</b><small>未接入真实课程音视频</small><button data-course-action="complete-lesson" data-product-id="${productId}" data-lesson-id="${lesson.id}">完成本节演示学习</button></section><section class="course-detail-block"><small>${esc(ctx,product.title)}</small><h1>${esc(ctx,lesson.title)}</h1><p>这里将展示课程要点、练习引导和真实授权媒体。当前为可操作的 Demo 学习页。</p></section><section class="course-detail-block"><h2>练习这一小步</h2><p>先在下一次互动里停半步，听完对方想表达的内容，再决定怎样回应。</p><button class="course-secondary" data-course-action="add-action" data-product-id="${productId}" data-lesson-id="${lesson.id}">加入行动</button></section></main>`;
  }
  function points(ctx) {
    const redeemable = ctx.model.CATALOG.filter(item => item.type === 'points');
    const today = ctx.today || new Date().toLocaleDateString('sv-SE',{timeZone:'Asia/Shanghai'});
    const checked = ctx.data.points.ledger.some(item => item.sourceType === 'checkin' && item.sourceDay === today);
    return `<main class="course-page">${title('成长积分','home')}<section class="course-points-head"><span>${icon('coin')}</span><div><small>当前可用</small><h1>${ctx.data.points.balance} <em>积分</em></h1><button data-course-action="route" data-route="points/ledger">查看明细 ›</button></div></section><section class="course-detail-block"><h2>兑换精选课程</h2>${redeemable.map(detailButton).join('')}</section><section class="course-detail-block"><h2>今天怎样获得积分</h2><ul class="course-task-list"><li><span><b>签到打卡</b><small>每天第一次登录可领取</small></span><strong>+2</strong><button data-course-action="checkin" ${checked ? 'disabled' : ''}>${checked ? '今日已签到' : '今日签到 · 去完成'}</button></li><li><span><b>确认对话小结</b><small>完成一次真实对话确认</small></span><strong>+4</strong><button data-course-action="route" data-route="home">去完成</button></li><li><span><b>完成课程学习</b><small>完成一节课程并回顾</small></span><strong>+6</strong><button data-course-action="route" data-route="courses">去完成</button></li><li><span><b>行动反馈</b><small>记录实践后的真实结果</small></span><strong>+8</strong><button data-course-action="route" data-route="growth">去完成</button></li><li><span><b>个人反思</b><small>写下今天的一次看见</small></span><strong>+6</strong><button data-course-action="route" data-route="growth">去完成</button></li></ul><p class="course-muted">每类任务每天首次完成可获得积分，每日最多 26 积分。</p></section></main>`;
  }
  function ledger(ctx) { return `<main class="course-page">${title('积分明细','points')}<section class="course-detail-block"><h2>可用积分 ${ctx.data.points.balance}</h2>${ctx.data.points.ledger.map(item => { const amount=Number(item.amount || 0); return `<div class="course-ledger"><div><b>${item.kind === 'earn' ? '获得成长积分' : item.kind === 'refund' ? '兑换退款' : '积分使用'}</b><small>${item.kind === 'spend' ? '课程积分兑换' : `有效至 ${String(item.expiresAt || '').slice(0,10) || '—'}`}</small></div><strong>${amount < 0 ? '−' : '+'}${Math.abs(amount)}</strong></div>`; }).join('') || '<div class="course-empty"><b>还没有积分明细</b><p>完成一次真实学习或反馈后会记录在这里。</p></div>'}</section></main>`; }
  function preferences(ctx) { const on = ctx.data.preferences.proactiveRecommendations; return `<main class="course-page">${title('课程推荐设置','settings')}<section class="course-detail-block"><h2>主动课程推荐</h2><p>控制聊天、行动反馈和成长回顾中的自动推荐；关闭后仍可自行到课堂找课。</p><button class="course-toggle ${on ? 'on' : ''}" data-course-action="toggle-recommendation" aria-pressed="${on}"><i></i><span>${on ? '已开启' : '已关闭'}</span></button></section></main>`; }
  function orders(ctx) { return `<main class="course-page">${title('课程订单','my-learning')}<section class="course-detail-block"><h2>全部订单</h2>${ctx.data.orders.slice().reverse().map(order => { const product=getProduct(ctx,order.productId); const label=order.status==='succeeded'?'已获取':order.status==='pending'?'待确认':order.status==='processing'?'处理中':'未完成'; return `<button class="course-card" data-course-action="order" data-order-id="${order.id}"><span class="course-cover type-${product?.type||'standard'}">${order.status==='succeeded'?icon('check'):icon('clock')}</span><span class="course-card-copy"><small>${String(order.createdAt).slice(0,10)} · ${label}</small><b>${esc(ctx,product?.title||'课程')}</b><em>${order.method==='points'?`${order.pointsCost} 积分` : money(order.amountFen)}</em></span><i>›</i></button>`; }).join('') || '<div class="course-empty"><b>还没有课程订单</b><p>获取课程后，记录会保存在这里。</p></div>'}</section></main>`; }
  function orderDetail(ctx,orderId) { const order=ctx.data.orders.find(item=>item.id===orderId), product=order&&getProduct(ctx,order.productId); if(!order||!product)return missing(ctx); const done=order.status==='succeeded', entitlement=ctx.data.entitlements.find(item=>item.orderId===order.id); const status=done?'已获取':order.status==='processing'?'处理中':order.status==='pending'?'待确认':'未完成'; return `<main class="course-page">${title('订单详情','courses/orders')}<section class="course-detail-block"><small>${status}</small><h2>${esc(ctx,product.title)}</h2><div class="course-ledger"><div><b>获取方式</b><small>${order.method==='points'?'成长积分兑换':'模拟支付'}</small></div><strong>${order.method==='points'?`${order.pointsCost} 积分`:money(order.amountFen)}</strong></div><div class="course-ledger"><div><b>订单编号</b><small>${esc(ctx,order.id)}</small></div></div>${entitlement?`<div class="course-ledger"><div><b>课程有效期</b><small>有效期至 ${String(entitlement.validUntil).slice(0,10)}</small></div><strong>已生效</strong></div>`:''}</section>${done?`<button class="course-primary full" data-course-action="learn" data-product-id="${product.id}">去学习</button>`:order.status==='pending'?`<button class="course-primary full" data-course-action="pay-existing" data-order-id="${order.id}">继续确认</button>`:''}</main>`; }
  function demo(ctx) { return `<main class="course-page">${title('课程演示工具','home')}<section class="course-detail-block"><h2>切换演示状态</h2><button class="course-secondary" data-course-action="demo-state" data-demo="new">新注册用户</button><button class="course-secondary" data-course-action="demo-state" data-demo="member">有效会员</button><button class="course-secondary" data-course-action="demo-state" data-demo="points">积分充足</button><button class="course-secondary" data-course-action="demo-state" data-demo="reset">清空课程数据</button><p class="course-demo-note">仅改变当前浏览器的课程演示数据。</p></section></main>`; }
  function missing(ctx) { return `<main class="course-page">${title('页面不存在','courses')}<div class="course-empty"><b>暂时没有找到这门课程</b><button class="course-primary" data-course-action="route" data-route="courses">回到简快课堂</button></div></main>`; }
  function render(route,ctx) { const base=String(route||'').split('?')[0], p=parts(route); let html; if(base==='courses') html=classroom(ctx); else if(base==='courses/list') html=list(ctx,route); else if(base.startsWith('courses/detail/')) html=detail(ctx,p[2]); else if(base.startsWith('courses/checkout/')) html=checkout(ctx,p[2],route); else if(base.startsWith('courses/result/')) html=result(ctx,p[2]); else if(base==='my-learning') html=learning(ctx); else if(base.startsWith('courses/learn/')) html=player(ctx,p[2],p[3]); else if(base==='points') html=points(ctx); else if(base==='points/ledger') html=ledger(ctx); else if(base==='courses/orders') html=orders(ctx); else if(base.startsWith('courses/order/')) html=orderDetail(ctx,p[2]); else if(base==='courses/preferences') html=preferences(ctx); else if(base==='demo-tools') html=demo(ctx); else html=missing(ctx); return scrollPage(html); }
  return {render,homeEntry,drawerEntry};
});
