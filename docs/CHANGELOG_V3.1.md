# V3.1 更新记录

- 修复 H5 在宽于 480px 的 ChatGPT 预览、WebView 和桌面浏览器中出现左右空白边的问题。
- 移除 `.app-frame` 的 480px 最大宽度限制。
- App 现在始终填满宿主视口，保持 `100dvh` 高度。
- 移除宽屏模式下的外部阴影和独立背景，避免右侧白边/米色边暴露。
- 不修改登录、AI 对话、正面解读、行动卡、侧栏、路由和返回逻辑。
