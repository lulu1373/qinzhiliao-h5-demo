# 首页布局验收

用户范围：只参考排版与布局，沿用现有产品文案、图标、配色、吉祥物和功能。

- 参考：用户附件 codex-clipboard-a10b3ba1-2109-44f7-9439-b16f8c1b3ce1.png，984 × 654 像素，局部入口区域，原始像素密度未知。
- 实现截图：`/tmp/qzl-home-layout.png`，390 × 844 手机预览；对比其中两列入口与三行对话卡区域，未将参考像素直接当作 CSS 尺寸。
- 实际浏览器：Codex In-app Browser，本地首页，登录后未开始对话状态。
- 同一输入内打开参考和实现截图作区域比较；该区域的文字、箭头、间距均清晰可见，无需额外细节放大。

## 检查结果

- 字体：沿用现有字体；标题和辅助文字保持两级，未使用参考里的新对话文案。
- 布局：两张等宽入口卡，三条全宽对话卡。390px 下入口列宽 173px、间距 12px；对话卡宽 358px、高 84px、间距 12px。保留现有辅助文案，因此卡片高度不直接照缩放后的参考高度复制。
- 颜色：保留当前米白底、白卡和语义图标配色；本次没有换肤。
- 图片：现有吉祥物和矢量图标沿用，不引入新图形资产。
- 内容：名称沿用“教育指南”；原来的三条场景文案、事件参数不变。测评与家庭档案保留在其后的小入口中。
- 交互：成长报告、教育指南及返回正常；第一条对话入口正常进入现有聊天并得到回复。另两条事件绑定与参数经代码复核未变。
- 响应式：360、390、430px，始终两列入口、三行对话，无横向溢出。
- 浏览器错误日志：无。
- 根目录与 latest 入口同步，代码复核未发现阻塞项。

## 对比历史

首轮按本次“仅布局”范围验收通过；参考里的新文案、井号图标、字号缩放不在本次范围，沿用产品已有样式。无 P0/P1/P2 待修问题。

final result: passed

## 里程碑功能验收（2026-09-09）

- 在已有成长页面增加「成长回顾 / 里程碑」切换，保持米白底、白卡、浅绿标签和时间线；首页布局不变。
- Codex In-app Browser 390 × 844 实际查看个人空态、示例时间线和填写页面；标题、标签、字段、按钮可读，长表单可滚动。
- 自动交互覆盖确认前不保存、刷新持久化、编辑保留 ID、移除与撤销、来源去重、来源更新核对、清除对话与撤销脱敏、示例隔离、跨年补记、成员归属、旧行动 ID 复用。
- 360、390、430px 时间线无横向溢出；原有报告、行动反馈、日期切换、来源查看及聊天草稿回归验证保留。
- 动态用户文字按文本转义；示例数据保持独立，来源提示不构成对家庭成员的评分。

## 共创体验落地验收（2026-09-09）

- 首页保留双列成长/社区入口与三行场景；缩短吉祥物区，卡牌集中到输入栏工具面板，修正继承大图标与空闲新回复提示。
- 社区使用单列经历、搜索与话题筛选；经验/方法/资讯标识清晰，小组、详情、评论草稿、发布预览可操作。内容和互动均明确为本机演示。
- 社区具体尝试可带入自己的行动，保留来源；关系变化会调整话术，已关联行动时保留原练习并创建新分支。
- 情绪支持可以暂停、结束、继续；练习历史可回看，反馈接受有帮助/没变化/更难/未试/不做。个人输入不会自动公开或成为里程碑。
- 四种练习卡支持填写、整理、翻面、确认收藏、修改原卡、再记一张；私人练习来源不伪装为聊天记录。
- Codex In-app Browser 390×844 实看首页、社区、情绪页、表达流程、卡背、卡面、成长页。控件文字、留白和触达正常，主要按钮保留暖橙且提高文字对比度。
- 自动浏览器检查覆盖 360/390/430 宽度下主要路由无横向溢出；460px 可视高度下长表单按钮可滚动到达（不等同于真机软键盘验收）。
- 已运行 111 项模型/视图检查；原成长与里程碑 31 项浏览器回归通过；新增 10 项浏览器用例分别通过，含草稿恢复、来源、反馈同步、去重、编辑、角色切换、删除私密练习及小屏布局。
- 构建统一同步 root/latest，并为资源添加内容指纹；历史 versions 目录不变。
