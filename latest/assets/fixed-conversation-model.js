(function(root, factory) {
  const api = factory();
  if (typeof module !== 'undefined' && module.exports) module.exports = api;
  root.QZLFixedConversationModel = api;
})(typeof window !== 'undefined' ? window : globalThis, function() {
  // Dialogue, report and card copy are taken verbatim from the approved reference H5 (解读卡H5).
  const CONTENT = {"version": "insight-1.0", "parents": ["小宝写作业怎么总是这么拖拉？明明题也不多，每次都弄到快十点。我催了好几次也没用，真是越看越来气。", "好，帮我看看吧。", "昨晚七点半开始写数学，前面的口算挺快。翻到应用题后就一直擦了写、写了擦，还拿橡皮在桌上摆弄，半天没往下写。", "我催了几次「快点写」，他还是没往下写，后来就说「你别一直催，我不知道先算什么，写错了又要擦」。我坐下来陪他读了一遍题，他才接着写，写完还是快十点了。", "对，就是这样。", "我最怕他养成拖拉的习惯，现在题少都这样，以后题多更写不完。我希望会做的能接着写，不用我一直催；不会的就早点说卡在哪里。"], "assistant": ["催了好几次，还是拖到快十点，难怪你又急又气。\n\n你愿意的话，我们可以挑最近一次一起看看：他在哪儿慢下来了，你提醒之后又发生了什么。我帮你把这些线索整理成一份解读。要是这会儿还气着，不想分析，也没关系，咱们可以先聊聊。", "好，我们先挑最近一次，不用你把整个晚上重新讲一遍。\n\n最近那次，他是从一开始就慢，还是写到某一部分才明显慢下来？\n\n当时你看到他具体在做什么？\n\n比如反复擦写、摆弄文具，或盯着题没动笔，说你记得的一两个动作就好。", "这个区别很有用：前面的口算能往下做，停顿主要出现在应用题。现在我们看的是一个具体卡住的片段，不只是笼统的「写作业慢」。\n\n看到他停着，你当时是怎么回应的？\n\n从你回应之后到这次作业结束，他的表现有怎样的变化？\n\n不必记住每句话，讲你记得的几个节点就好：比如仍然停着、说出了困难，或者后来继续写了。", "从你说的经过看，有个变化值得留意：几次催促后，他仍停着；你陪他读题后，他才继续往下写。\n\n我把这一次的经过串起来，你看看有没有哪里没说准：\n\n1. **场景**：昨晚七点半开始写数学作业，前面的口算比较顺，到了应用题明显慢下来。\n2. **过程**：他反复擦写、摆弄橡皮；你催了几次「快点写」，他还是没往下写，后来告诉你「不知道先算什么，写错了又要擦」。你随后坐下来，陪他读了一遍题。\n3. **结果**：一起读题后，他继续写了，作业最终完成，但已经接近十点。\n\n我这样梳理，符合当时的实际情况吗？\n\n如果有不准确或遗漏的地方，你可以进行补充。", "好，那我们就按这个经过往下看。你刚才说越看越来气，最后我也想听听你这边。\n\n**这件事最让你在意或担心的是什么？**\n\n- 每天拖到很晚，怕影响睡觉和休息。\n- 怕拖拉变成习惯，以后事情更多时更应付不过来。\n- 每天都得陪着、催着，自己的时间和精力被耗住了。\n\n**你最希望先看到哪一点变化？**\n\n- 会做的能接着写，不用反复催。\n- 不会的早点说卡在哪里，不要一直停着。\n- 作业早点结束，不再每天拖到那么晚。\n\n这些只是例子，挑贴近你的说就好，也可以自己说，不用逐项回答。", "你担心的不只是昨晚写得晚，而是这样拖下去会养成习惯，以后题多了更写不完。你希望先看到的变化也很具体：会做的接着写，不用你反复催；不会的早点说卡在哪里。\n\n现在我已经搜集到了足够的信息。接下来，我将为你生成《解读卡》，帮助你梳理这次事件。"], "result": "# **解读卡·亲子翻译**\n\n### ① 这次发生了什么\n\n- **事件场景**：昨晚七点半，小宝开始写数学作业，前面的口算进行得比较顺利。\n\n- **主要冲突**：你希望作业继续推进，但几次提醒“快点写”后，他仍停着。后来你陪他读题，他才继续，最终完成时已接近十点。\n\n- **孩子客观的表现**：到了应用题，他反复擦写、摆弄橡皮，没有继续往下写，并说：“你别一直催，我不知道先算什么，写错了又要擦。”\n\n- **家长期待的结果**：你担心拖拉形成习惯，以后题多了更写不完。你希望会做的题能接着写，不用反复催；不会的时候，能早点说出卡在哪里。\n\n### ② 孩子行为背后的信息\n\n1. **能力卡点：可能还没找到做题的第一步**\n\n   前面的口算较顺，停顿主要出现在应用题，加上他明确说“不知道先算什么”，这次更值得先看的是：他能否理清题意、确定第一步。\n\n   因此，“写得慢”不宜直接等同于“不自觉”。不过，这些线索还不能说明他每次拖拉都出于同一个原因。\n\n2. **关系信号：他在回应催促，也在表达自己的困难**\n\n   “你别一直催”表达了他不希望持续被催；紧接着的“不知道先算什么”，又提供了具体困难的信息。\n\n   这句话不只有拒绝的意味，也包含一个可以继续了解他的入口。至于他是否感到紧张、担心被批评，目前还不能确定。\n\n### ③ 你们怎样互相影响\n\n1. **看到互动模式**\n\n   这次前半段出现了“他停着不写——你提醒快一点—他仍停着——你再次提醒”的来回。催促在推动速度，但还没有解决他表达的“下一步怎么做”的困难。\n\n   后来你改为陪他读题，他继续往下写，这段互动让事件出现了转折。\n\n2. **如果这种互动反复延续**\n\n   如果以后遇到类似情况，仍主要依靠反复催促，具体的做题困难可能迟迟没有得到处理。同类问题反复出现时，你们也容易把注意力放在“为什么还不快点”，而不是弄清“究竟卡在哪里”。\n\n\n### ④ 换个角度看这件事\n\n**正面重述**\n\n- **原本的说法**：“他写作业怎么总是这么拖拉？”\n\n- **可以尝试的新说法**：“这次他在应用题上卡住了，但他已经能说出自己不知道先算什么，也在一起读题后继续往下做。能够表达困难、借助帮助继续，是可以支持他往前走的起点。”\n\n这个理解保留了需要改善的地方，也看见了孩子已经做到的部分；并不意味着作业拖到很晚就不需要处理。\n\n**督导提醒**\n\n1. **先辨认一个具体卡点。** 下次再次停住时，可以先了解他卡在哪一步，把注意力从单纯提醒速度，转向眼前的困难。\n\n2. **留意帮助后能否自己继续。** 一起弄清卡点后，给他尝试下一步的空间；如果仍停着，再了解剩下的困难，不必马上接管整份作业。", "summary": [{"label": "本次片段", "text": "昨晚口算较顺，到了应用题，小宝反复擦写、停着没往下做，最终接近十点才完成。"}, {"label": "值得记住的理解", "text": "这次的慢，可能与找不到做题的第一步有关，不宜直接等同于“不自觉”。他说“不知道先算什么”，也在一起读题后继续了。"}, {"label": "你的担心与期待", "text": "你担心拖拉养成习惯，以后题多更写不完；希望他会做的接着写，不会的早点说，不用反复催。"}, {"label": "下次可以试试", "text": "再停住时，先把“快点写”换成了解具体卡点：“这道题，你卡在哪一步？”"}], "hints": [["那会儿他有没有叹气、揉眼睛等情绪或身体上的表现？", "当时周围有没有电视、聊天声等可能分散注意的动静？"], ["提醒时，你是在旁边陪着，还是偶尔过去看一眼？", "如果后来继续写了，他能自己往下做，还是需要你一道一道带着？"], ["在这次聊之前，你是怎样理解他的「慢」的？", "有没有需要一起顾及的现实安排，比如你的工作时间或家里的作息？"]], "draftHints": [["那会儿他的情绪或身体表现是……", "当时周围的环境是……"], ["提醒他时，我当时在……", "后来继续写的时候，他……"], ["之前我对他的“慢”的理解是……", "需要一起顾及的现实安排是……"]]};
  const P = CONTENT.parents, A = CONTENT.assistant;
  const STEPS = ['当时情况','双方回应','你的想法'];
  const LABELS = {invite:'邀请',free:'先聊聊',scene:'当时情况',interaction:'双方回应',confirm:'核对经过',expectation:'你的想法',result:'解读'};
  const PRESET = {seed:P[0],accept:P[1],scene:P[2],interaction:P[3],confirm:P[4],expectation:P[5]};
  const REPLIES = {accept:P[1],confirm:P[4],decline:'我现在不想分析，就是很烦。'};
  const DECLINE_REPLY = '那咱们先不分析。你已经提醒了好几次，事情还是拖到很晚，这股气不用马上压下去。你想吐槽刚才那一幕，或者就说说今天有多累，都可以，我听着。';
  const FREE_REPLY = '这段话先留在这里，不用急着分析。当前是预设体验，暂时不能对自由输入作进一步解读；想继续作业示例时，可以选择「现在帮我看看」。';
  const TOPIC_NOTE = '本版先保留这个话题；进一步自由对话尚未接入。';
  const SUMMARY = CONTENT.summary.map(item => ({title:item.label, body:item.text}));
  const HINT_INDEX = {scene:0, interaction:1, expectation:2};
  const clean = s => String(s || '').normalize('NFKC').replace(/[\s\p{P}\p{S}]/gu, '');
  const same = (a, b) => clean(a) === clean(b);
  const esc = s => String(s ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const inline = s => esc(s).replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>');
  function markdown(source){
    const lines=String(source).split('\n');let i=0,out='';
    while(i<lines.length){
      if(!lines[i].trim()){i++;continue;}
      const heading=lines[i].match(/^(#{1,3}) (.*)/);
      if(heading){out+=`<h${heading[1].length}>${inline(heading[2])}</h${heading[1].length}>`;i++;continue;}
      const list=lines[i].match(/^(\d+\.|-) (.*)/);
      if(list){const ordered=list[1]!=='-',tag=ordered?'ol':'ul';out+=`<${tag}>`;
        while(i<lines.length){const item=lines[i].match(/^(\d+\.|-) (.*)/);if(!item||(item[1]!=='-')!==ordered)break;const buf=[item[2]];i++;
          while(i<lines.length){if(/^\s{2,}\S/.test(lines[i])){buf.push(lines[i].trimStart());i++;}else if(!lines[i].trim()){let j=i+1;while(j<lines.length&&!lines[j].trim())j++;if(/^\s{2,}\S/.test(lines[j]||'')){buf.push('');i=j;}else{i=j;break;}}else break;}
          out+=`<li>${markdown(buf.join('\n'))}</li>`;
        }out+=`</${tag}>`;continue;
      }
      const para=[];while(i<lines.length&&lines[i].trim()&&!/^(#{1,3} |\d+\. |- )/.test(lines[i]))para.push(lines[i++]);out+=`<p>${inline(para.join('\n')).replace(/\n/g,'<br>')}</p>`;
    }return out;
  }
  const step = (flow, messages, patch) => ({...flow, ...patch, out: messages});
  function createFlow() { return {stage:'invite', paused:false, result:null, resumeStage:null, resumeCount:null, claimed:false, out:[{role:'user', text:P[0]}, {role:'ai', text:A[0]}]}; }
  function accept(flow, text) { if (!flow || !['invite','free'].includes(flow.stage)) return step(flow, [], {}); return step(flow, [{role:'user', text:text || P[1]}, {role:'ai', text:A[1]}], {stage:'scene'}); }
  function decline(flow, text) { if (flow?.stage !== 'invite') return step(flow, [], {}); return step(flow, [{role:'user', text:text || REPLIES.decline}, {role:'ai', text:DECLINE_REPLY}], {stage:'free'}); }
  function confirm(flow, text) { if (flow?.stage !== 'confirm') return step(flow, [], {}); return step(flow, [{role:'user', text:String(text || '').trim() || P[4]}, {role:'ai', text:A[4]}], {stage:'expectation'}); }
  function completeResult() { return {markdown:CONTENT.result, summary:SUMMARY.map(item => ({...item})), partial:false, stage:3, intro:A[5]}; }
  function partialResult(stage) {
    const n = {invite:1, free:1, scene:1, interaction:2, confirm:2, expectation:3}[stage] || 1;
    const hasScene = ['interaction','confirm','expectation'].includes(stage), hasInteraction = ['confirm','expectation'].includes(stage);
    const scene = hasScene ? '昨晚七点半开始写数学，口算较顺，到应用题后反复擦写、摆弄橡皮，停着没有往下写。' : '家长说作业经常拖到快十点，反复催促没有奏效；最近一次的具体片段尚未补充。';
    const current = hasInteraction ? '几次催促后仍停着，一起读题后继续，最终接近十点完成。' : '双方具体回应和本次结果还没有了解完整。';
    const insight = hasInteraction ? '“不知道先算什么”和一起读题后继续，是值得关注的具体线索；还不能据此判断每次拖拉的原因。' : hasScene ? '目前知道口算与应用题的表现存在差异，但还不能确定卡住的原因。' : '目前信息不足，还不能判断孩子行为背后的原因。';
    const thoughts = '你的担心与具体期待尚未完整了解。';
    const action = hasScene ? '下次停住时，可以先了解他卡在哪一步。' : '可以从最近一次看见的具体动作开始，暂不急着判断原因。';
    return {partial:true, stage:n, intro:'我先根据目前的信息，为你整理一份《解读卡》，尚不能确定的部分会标明。',
      markdown:`# **解读卡·亲子翻译**\n\n### ① 这次发生了什么\n\n- **事件场景**：${scene}\n\n- **主要冲突**：${current}\n\n- **孩子客观的表现**：${hasScene ? '反复擦写、摆弄橡皮，停着没有往下写。' : '尚未获得具体行为信息。'}\n\n- **家长期待的结果**：${thoughts}\n\n### ② 孩子行为背后的信息\n\n${insight}\n\n### ③ 你们怎样互相影响\n\n${hasInteraction ? '催促后仍停着、一起读题后继续，是这次已知的互动变化。单次片段不能确定长期模式。' : '目前不足以还原互动来回，不推断固定沟通模式或未来风险。'}\n\n### ④ 换个角度看这件事\n\n暂时不把“慢”等同于“不自觉”，先保留还不知道的部分。\n\n**督导提醒**\n\n${action}`,
      summary:[{title:'本次片段',body:scene},{title:'值得记住的理解',body:insight},{title:'你的担心与期待',body:thoughts},{title:'下次可以试试',body:action}]};
  }
  function finish(flow, messages, partial) { const result = partial ? partialResult(flow.stage) : completeResult(); return step(flow, [...messages, {role:'ai', text:result.intro, result}], {stage:'result', result, resumeStage:partial ? flow.stage : null}); }
  function answer(flow, text) {
    const value = String(text || '').trim();
    if (!flow || flow.paused || !value) return step(flow, [], {});
    const user = {role:'user', text:value};
    if (flow.stage === 'invite') return [REPLIES.decline, '我想先聊聊'].some(x => same(value, x)) ? decline(flow, value) : accept(flow, value);
    if (flow.stage === 'free') return [P[1], '好', '一起看看', '帮我看看', '现在帮我看看'].some(x => same(value, x)) ? accept(flow, value) : step(flow, [user, {role:'ai', text:FREE_REPLY}], {});
    if (flow.stage === 'scene') return step(flow, [user, {role:'ai', text:A[2]}], {stage:'interaction'});
    if (flow.stage === 'interaction') return step(flow, [user, {role:'ai', text:A[3]}], {stage:'confirm'});
    if (flow.stage === 'confirm') return confirm(flow, value);
    if (flow.stage === 'expectation') return finish(flow, [user], false);
    return step(flow, [], {});
  }
  function early(flow) { if (!flow || !['invite','free','scene','interaction','confirm','expectation'].includes(flow.stage)) return step(flow, [], {}); return finish({...flow, paused:false}, [], true); }
  function continuePartial(flow) { if (flow?.stage !== 'result' || !flow.result?.partial || !flow.resumeStage) return step(flow, [], {}); return step(flow, [], {stage:flow.resumeStage, result:null, resumeStage:null}); }
  function claim(flow) { if (flow?.stage !== 'result' || !flow.result) return flow; return {...flow, stage:'reveal_back', claimed:true}; }
  function pause(flow) { if (!flow || ['result','reveal_back','done'].includes(flow.stage)) return flow; return {...flow, paused:true}; }
  function resume(flow) { return flow ? {...flow, paused:false} : flow; }
  function expected(flow) { return flow && !flow.paused ? ({scene:P[2], interaction:P[3], expectation:P[5]}[flow.stage] || '') : ''; }
  function hints(flow) { const i = flow && !flow.paused ? HINT_INDEX[flow.stage] : undefined; return i === undefined ? null : {questions:CONTENT.hints[i], drafts:CONTENT.draftHints[i]}; }
  function progress(flow) {
    if (!flow || ['invite','free','reveal_back','done'].includes(flow.stage)) return null;
    const partial = flow.stage === 'result' && flow.result?.partial;
    const complete = flow.stage === 'result' && !partial;
    const current = partial ? flow.result.stage : ({scene:1, interaction:2, confirm:2, expectation:3, result:3}[flow.stage] || 1);
    const title = flow.paused ? '解读已暂停' : flow.stage === 'result' ? (partial ? '目前的理解 · 部分信息待补充' : '这次解读已整理好') : '一起看懂这次发生的事';
    return {title, complete, current, early:flow.stage !== 'result' && !flow.paused, steps:STEPS.map((label, i) => ({label, index:i + 1, state:complete || i + 1 < current ? 'done' : i + 1 === current ? 'current' : 'todo'}))};
  }
  return {CONTENT, STEPS, LABELS, PRESET, REPLIES, DECLINE_REPLY, FREE_REPLY, TOPIC_NOTE, SUMMARY, markdown, esc, createFlow, accept, decline, answer, confirm, early, continuePartial, claim, pause, resume, expected, hints, progress, completeResult, partialResult};
});
